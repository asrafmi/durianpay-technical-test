export enum PaymentStatus {
  COMPLETED = 'completed',
  PROCESSING = 'processing',
  FAILED = 'failed',
}

export enum StatusFilter {
  ALL = '',
  COMPLETED = 'completed',
  PROCESSING = 'processing',
  FAILED = 'failed',
}
